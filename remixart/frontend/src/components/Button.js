import React, { Component } from "react";

class Button extends Component {
  constructor(props) {
    super(props);

    this.state = {
      userName: "Archita",
    };
  }

  render() {
    return <div></div>;
  }
}

export default Button;
